css
    tag{}       general
    .class{}    para las class
    #id{}       para las id

"" comillas dobles para darle propiedad de texto
'' comillas lo mismo de antes, pero al usarlas una dentro de otra sirve para poder imprimir dentro de un texto
´´ backtick se usa ´${} para concatenar variables con texto/ */
console.log(`${saludo} "hola" ${x}`);

let CamelCase   //cada palabra inicia con mayuscula -                                                       Clases de objetos
let lowerCamelCase //la primera minuscula, despues mayuscula -                                              variables y funciones
let snake_case //es espacio se cambia por guion bajo                                                        
let SCREAMING_SNAKE_CASE //para variables de entorno todo mayuscula o llamada UPPER_SNAKE_CASE              variables de entorno o globales
let kebab-case //usa un guion medio que no admite js                                                             clases de css
let strNombre //notacion hungara un prefijo para definir el tipo str=string                                 variables
//se acostumbra el kebab-case para css y el lowerCamelCase para js

arreglo se puede usar para matrices [[a,b],[c,d]]
objetos
datos = {nombre: "JUAN", apellido: "Perez", Alias: "JP"} nombre seria una propiedad de este objeto
datos.edad=50 agrega la propiedad edad con el valor 50

array.lenght;           da el largo
array.type;             da el tipo de dato
array.charAt(X)         el caracter en la posicion X
array.split("Y")        separa en el valor Y

Para arreglos (array)
variable.push() agrega otro elemento
variable.pop() elimina el ultimo elemento
variable.shift() elimina el primer elemento del array
variable.unshift() agrega un valor al inicio de un array
variable.splice(x,y) desde la posicion X elimina Y
variable.includes(x,y) si el arreglo incluye X elementos, si se especifica Y empieza a buscar desde esa posicion
variable.foreach(Funcion) ejecuta Funcion para cada elemento del arreglo

let variable= array.shift(); asigna a la variable el primer valor del array

let accionBotonArea=document.getElementById("botonArea");
accionBotonArea.addEventListener("click",()=>validarDiametro()); 
el "()=>" se usa para que no se ejecute apenas se cargue la pagina
Arrow function
const precioIva = precio => precio*1,19 para multiples lineas se usa llave

if(condicion)
{}
else
{};
while (condicion){};
do{} while(condicion);
for(variable inicial; condicion; variable inicial++){};
break rompe el ciclo

Funciones
Entra el valor no la variable
funcion anonima cuando se define de inmediato con variables
const saludo=function(){ lo que ejecuto}

function nombre (a,c,b...)  se puede definir un valor por defecto de las entradas por ej a=3, si no se especifia ese valor se toma por defecto.
{que hace la funcion
return  se usa para que la funcion devuelva un valor por ejemplo return a+b de la funcion suma se puede asignar a una variable ej let resultado=function asignara lo que entrega el return
}
se puede asignar a una variable el valor de una funcion let var = function